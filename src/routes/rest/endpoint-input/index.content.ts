import { t, type Dictionary } from 'intlayer';

const pageContent = {
  key: 'enpoint-input',
  content: {
    label: t({
      en: 'Endpoint URL',
      ru: 'Эндпоинт',
    }),
  },
} satisfies Dictionary;

export default pageContent;
